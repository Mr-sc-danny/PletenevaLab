# CAVER Frame Extraction

This script is part of the same CAVER workflow family as the PyMOL view scripts.
It is kept separate because it extracts frames instead of rendering them.
The logic is closely related but not identical to the viewer scripts.
