# EPR Angle Scripts

These variants were merged into [methionine_ligand_orientation.py](methionine_ligand_orientation.py).

The merged script keeps the same ligand-plane analysis and exposes the dataset choice through `--model`.
