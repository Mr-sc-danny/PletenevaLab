# Contact Map Analysis

This family was collapsed into [contact_map_analysis.py](contact_map_analysis.py).

The merged script handles both the full contact-map calculation and the focused heatmap view through `--mode compute`, `--mode focus`, or `--mode both`.