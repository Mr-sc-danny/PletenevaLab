# contact_map_neat_figure_only

The focused heatmap variants were merged into [../contact_map_analysis.py](../contact_map_analysis.py).

Use `--mode focus` to render the compact heatmap view from the combined script.
