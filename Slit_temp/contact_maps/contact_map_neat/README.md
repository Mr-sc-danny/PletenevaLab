# contact_map_neat

The two contact-map variants were merged into [../contact_map_analysis.py](../contact_map_analysis.py).

That single script now supports both datasets and the focused heatmap view through command-line modes.
