# WH Scripts

The pairwise WH variants were merged into [wh_distance_analysis.py](wh_distance_analysis.py).

The merged script supports the simple pairwise form and the 2042f minimum-heavy-atom form through `--model`.
The more complex `dist_meas` multi-dataset scripts remain separate.
